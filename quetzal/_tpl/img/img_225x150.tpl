{{ image rendition="sectionthumbtablet" }}
    <img class="visible-tablet" src="{{ $image->src }}" width="{{ $image->width }}" height="{{ $image->height }}" alt="{{ $image->photographer}}: {{ $image->caption }}" />
{{ /image }}
